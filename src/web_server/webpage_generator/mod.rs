mod date;
mod secret_manager;

pub fn generate_webpage() -> String {
    // get the secret
    // if the secret is ready to be divulged, generate that webpage
    // else, print a generic "not ready" webpage
    "(mock) Webpage HTML".to_owned()
}
